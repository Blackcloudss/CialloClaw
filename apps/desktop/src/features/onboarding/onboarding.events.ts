export const desktopOnboardingEvents = Object.freeze({
  sessionChanged: "desktop-onboarding:session-changed",
});

export const desktopOnboardingLocalEvents = Object.freeze({
  sessionChanged: "desktop-onboarding:local-session-changed",
});
